<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:core="clr-namespace:Surveys.Core"
             x:Class="Surveys.SurveysView"
             Title="Encuestas">
    <ContentPage.Resources>
        <ResourceDictionary>
            <core:Data x:Key="data" />

            <core:TeamColorConverter x:Key="PlayeraConverter" />

            <DataTemplate x:Key="SurveyDataTemplate">
                <ViewCell>
                    <StackLayout>
                        <Label Text="{Binding Name}"
                               FontSize="24" />
                        <Label Text="{Binding Birthdate, StringFormat='{}{0:dd/MM/yyyy}'}" />
                        <Label Text="{Binding FavoriteTeam}"
                               TextColor="{Binding FavoriteTeam, Converter={StaticResource PlayeraConverter}}" />
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ResourceDictionary>
    </ContentPage.Resources>
    
    <Grid BindingContext="{StaticResource data}"
          Margin="10" >
        <Grid.RowDefinitions >
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button Text="+"
                Clicked="AddSurveyButton_Clicked" />

        <ListView ItemsSource="{Binding Surveys}"
                  SelectedItem="{Binding SelectedSurvey, Mode=TwoWay}"
                  ItemTemplate="{StaticResource SurveyDataTemplate}"
                  Grid.Row="1"
                  Margin="10"
                  HasUnevenRows="True" />
    </Grid>
</ContentPage>